<!DOCTYPE html>
<html>
<head>
    <title>SEMINAR.CO.ID</title>
</head>
<body>
    <h1>{{ $title }}</h1>
    <small style="margin: 10px 0px">{{ $header }}</small>
    <p>{!! $content !!}</p>
    <small style="margin-top: 10px; text-align: center">{{ $footer }}</small>
</body>
</html>